# apps/www — marketing site

**Owner:** Agent 6 (optional, Phase 2)

Placeholder. Nothing here yet beyond a workspace entry so the `apps/*` glob
resolves.

If the landing prototype `ovation-landing.html` is dropped at the repo root,
match its spirit — not its markup — when building this out. Use the design
tokens from `@ovation/core` (`@ovation/core/design/tokens.css` and the Tailwind
preset) so the marketing site and the product share one palette.

Suggested port when it becomes a Next app: **3003**.
